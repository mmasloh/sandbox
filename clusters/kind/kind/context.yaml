# Context specific to a kind cluster named 'kind'

context:

  cluster:
    name: kind
    domain: kind.local

  apiServer:
    portOnLocalhost: 5443    # <== To configure

  ingress:
    url: ingress.kind.local

