
deployments:

  - name: podinfo
    enabled: true
    component: podinfo
    namespace: podinfo
    vars:
      allowedGroups:
        - podinfo
        - devs
        - system:masters
    dependsOn:
      - deployment: ingress-nginx
        module: main

  - name: podinfo2
    enabled: false
    component: podinfo
    namespace: podinfo2
    vars:
      allowedGroups:
        - devs
      ingress: podinfo2
    dependsOn:
      - deployment: ingress-nginx
        module: main

