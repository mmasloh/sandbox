
deployments:

  - name: podinfo1
    enabled: true
    component: podinfo
    namespace: podinfo
    vars:
      ingress: podinfo1
      allowedGroups:
        - podinfo
        - devs
        - system:masters
      dex:
        connector: skas
    dependsOn:
      - deployment: ingress-nginx
        module: main

  - name: podinfo2
    enabled: true
    component: podinfo
    namespace: podinfo
    vars:
      allowedGroups:
        - devs
      ingress: podinfo2
      dex:
        connector: openldap
    dependsOn:
      - deployment: ingress-nginx
        module: main

