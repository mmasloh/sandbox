
deployments:

  - name: podinfo
    enabled: true
    component: podinfo
    namespace: podinfo
    vars:
      allowedGroups:
        - skas-admin
    dependsOn:
      - deployment: ingress-nginx
        module: main
