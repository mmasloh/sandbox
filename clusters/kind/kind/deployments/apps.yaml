
deployments:

  - name: podinfo
    enabled: false
    component: podinfo
    namespace: podinfo
    vars:
      allowedGroups:
        - skas-admin
    dependsOn:
      - deployment: ingress-nginx
        module: main
