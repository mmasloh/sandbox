# Context specific to a kind cluster named 'kubo1'

context:

  cluster:
    name: kubo1
    domain: kubo1.local

  apiServer:
    portOnLocalhost: 7443    # <== To configure

  ingress:
    url: ingress.kubo1.local

