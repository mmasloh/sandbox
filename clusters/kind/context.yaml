# Context specific to all 'kind' clusters

context:

  storageClass:
    data: standard
    workspace: standard

  networkPoliciesSupport: false

  skas:
    skusers:
      bindingByName:
        sa.podinfo:
          user: sa
          group: podinfo
        sergea.podinfo:
          user: sergea
          group: podinfo


